const NavbarRouteName = {
  PROFILE: '/profile',
  HOME: '/',
  ABOUT: '/about',
  INVENTORY: '/inventory',
  HIERARCHY: '/hierarchy'
}

const RouteName = {
  ...NavbarRouteName,
  PRODUCTS: '/products'
}

export { NavbarRouteName, RouteName }
